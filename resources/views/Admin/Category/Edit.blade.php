{{-- @extends('layouts.admin')
@section('content')
    <form action="{{ route('category.postedit', ['id' => $menu->menu_id]) }}" method="post">
        <div class="form-group">
            <label for="id">
                <input type="text" value="{{ old('menu_id') || $menu->menu_id }}">
            </label>
        </div>
    </form>
@endsection --}}
